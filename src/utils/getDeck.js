export default (data) => {
  let copy = Object.values(data);
  let cards = [];
  console.log("data");
  console.log(data);
  console.log("cards");
  console.log(copy);

  //copy.foreach((card) => cards.push(card));
  return cards;
};
